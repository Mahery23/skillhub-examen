<?xml version="1.0" encoding="UTF-8"?>
<?import javafx.scene.control.*?>
<?import javafx.scene.layout.*?>
<?import javafx.geometry.Insets?>

<VBox xmlns:fx="http://javafx.com/fxml"
      fx:controller="com.example.authserver.fx.RegisterController"
      styleClass="root-pane"
      alignment="CENTER"
      spacing="0">

    <!-- Header -->
    <VBox styleClass="header" alignment="CENTER" spacing="6">
        <Label text="🔐" styleClass="header-icon"/>
        <Label text="Authentification Forte" styleClass="header-title"/>
        <Label text="TP3 — Protocole HMAC-SHA256" styleClass="header-subtitle"/>
    </VBox>

    <!-- Form -->
    <VBox styleClass="form-card" spacing="16">
        <padding><Insets top="30" right="40" bottom="30" left="40"/></padding>

        <Label text="Créer un compte" styleClass="form-title"/>

        <VBox spacing="6">
            <Label text="Adresse email" styleClass="field-label"/>
            <TextField fx:id="emailField" promptText="exemple@email.com" styleClass="text-field"/>
        </VBox>

        <VBox spacing="6">
            <Label text="Mot de passe" styleClass="field-label"/>
            <PasswordField fx:id="passwordField" promptText="••••••••" styleClass="text-field"/>
        </VBox>

        <VBox spacing="6">
            <Label text="Confirmer le mot de passe" styleClass="field-label"/>
            <PasswordField fx:id="confirmPasswordField" promptText="••••••••" styleClass="text-field"/>
        </VBox>

        <Label fx:id="statusLabel" text="" styleClass="status-label" wrapText="true"/>

        <Button fx:id="registerButton" text="S'inscrire"
                styleClass="btn-primary"
                maxWidth="Infinity"
                onAction="#handleRegister"/>

        <HBox alignment="CENTER" spacing="4">
            <Label text="Déjà un compte ?" styleClass="link-text"/>
            <Hyperlink text="Se connecter" onAction="#goToLogin" styleClass="hyperlink"/>
        </HBox>
    </VBox>

</VBox>
